namespace com.frederickampsup.myApp

#allow argv;
#allow io;
#allow string;

import com.frederickampsup.myApp.other_file;

fn main (argv: list[string]): int {
    other_file.doNothing();
    return 0;
} (argv) -> exit;